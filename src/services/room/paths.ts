import { BOOKING_ENDPOINT } from "../endpoint";

export const ROOMS_PATH = `${BOOKING_ENDPOINT}/partner/rooms`;
export const ROOM_BY_ID = `${BOOKING_ENDPOINT}/partner/rooms/:id`;
export const BOOKING_LIST = `${BOOKING_ENDPOINT}/partner/rooms/:id/bookings`;
